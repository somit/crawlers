class link(object):
  def __init__(self, parentUrl, count, url):
    self.parentUrl = parentUrl
    self.count = count
    self.url = url
  
  def __repr__(self):
    return self.parentUrl + " <-parent : child->  " + self.url
   
